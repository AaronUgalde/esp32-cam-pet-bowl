// ──────────────────────────────────────────────────────────────
//  secrets.h.example  —  ÚNICO archivo de credenciales WiFi / WebSocket
//  Copia a secrets.h en ESTA MISMA carpeta (raíz del repo).
//  Usado por: robot_hub, hub_simulated, send_images_wifi, ver_ip
// ──────────────────────────────────────────────────────────────
#pragma once

#define WIFI_SSID "tu_red_wifi"
#define WIFI_PASS "tu_contraseña_wifi"

// IP de tu PC en la LAN (ipconfig → IPv4). Solo lo usa el ESP32 dev (hub).
// NO uses 0.0.0.0 ni espacios al inicio/final.
#define WS_HOST "192.168.0.21"
#define WS_PORT 8765
